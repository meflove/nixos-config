---
description: Comprehensive code review (security, performance, architecture) via the code_review skill
---

Load the `code_review` skill using the `skill` tool, then follow its instructions to review the target below.

Everywhere the skill refers to `$ARGUMENTS`, substitute the arguments passed to this command:

```
$ARGUMENTS
```

If no arguments were provided, ask the user which files, directories, or changes to review before starting.
